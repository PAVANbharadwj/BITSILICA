const int ldrPin = 34; // AO pin
int ldrValue = 0;

void setup() {
  Serial.begin(115200);
}

void loop() {
  ldrValue = analogRead(ldrPin); // Read analog value
  Serial.println(ldrValue);      // Print light intensity
  delay(500);
}
